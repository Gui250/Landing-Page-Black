{
  "name": "landing-page-black-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo para Landing Page Black - Frontend (React/Vite) + Backend (Express)",
  "scripts": {
    "install:all": "npm install && npm install --prefix landing-page-black && npm install --prefix backend",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix landing-page-black",
    "vercel-build": "bash scripts/vercel-build.sh",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix landing-page-black",
    "setup:vercel": "node scripts/setup-vercel-env.js",
    "config:vercel": "./scripts/config-vercel.sh"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "vercel": "^48.10.11"
  },
  "engines": {
    "node": ">=18.x"
  }
}
