{
  "version": 2,
  "buildCommand": "npm install --prefix backend && npm install --prefix landing-page-black && npm run build --prefix landing-page-black",
  "outputDirectory": "landing-page-black/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}

